'use client'

// Decorative background removed per P1 brand audit — blobs and floating
// symbols raised cognitive load without adding information. Kept as empty
// export so any future re-import compiles without changes to layout.tsx.
export function MathBackground() {
  return null
}
