# Error Tracking
sentry-sdk[fastapi]>=2.0

# Logging
python-json-logger>=2.0

# Core Web Framework
fastapi>=0.115.0  # 0.115+ supports anyio>=4
uvicorn[standard]>=0.30.0
pydantic[email]==2.5.0
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
PyJWT[crypto]>=2.8.0  # Clerk JWT verification (RS256 + JWKS)
slowapi>=0.1.9         # Per-user/IP rate limiting
stripe>=7.0.0          # Payment processing for session credits
deepgram-sdk>=3.0.0    # STT for voice sessions (Phase 3)
elevenlabs>=1.0.0      # TTS for voice sessions (Phase 3)

# HTTP Client
httpx>=0.27.0  # 0.27+ supports anyio>=4, required by mcp>=1.0.0
aiofiles==23.2.1
requests==2.31.0

# Mathematics & Problem Generation
sympy==1.12
antlr4-python3-runtime==4.11.1  # sympy.parsing.latex (MathLive answer parsing)

# Document extraction (Phase 3: PDF upload → Claude Vision)
# pdfplumber (MIT) replaces PyMuPDF (AGPL-3.0, incompatible with commercial SaaS)
pdfplumber>=0.11

# LLM Integration
anthropic>=0.40.0  # Claude API — primary LLM provider (see ADR-002)
mcp>=1.0.0         # MCP client — Wolfram/GeoGebra backend routing (Phase 3)
openai==1.3.8      # Fallback; deprecated in Phase 12

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
# anyio is auto-resolved from dependencies (httpx 0.27+ and mcp both require anyio>=4)

# Email Validation
email-validator==2.1.0

# Observability
sentry-sdk[fastapi]>=2.0.0
python-json-logger>=2.0.0
