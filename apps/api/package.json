{
  "name": "@math-platform/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn api:app --reload --port 8000",
    "test": ".venv\\Scripts\\python -m pytest tests/ -v",
    "test:coverage": ".venv\\Scripts\\python -m pytest tests/ -v --cov",
    "build": "echo \"Python backend — no build step\""
  }
}
